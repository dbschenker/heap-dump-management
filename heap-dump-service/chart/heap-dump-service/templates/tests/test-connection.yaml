apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "heap-dump-service.fullname" . }}-test-connection"
  labels:
    {{- include "heap-dump-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "heap-dump-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
