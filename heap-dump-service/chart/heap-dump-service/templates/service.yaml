apiVersion: v1
kind: Service
metadata:
  name: {{ include "heap-dump-service.fullname" . }}
  labels:
    {{- include "heap-dump-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "heap-dump-service.selectorLabels" . | nindent 4 }}
